services:
  seismic-agent:
    build:
      context: .
    image: seismic-agent-v0.2:arm64
    container_name: seismic-agent-v0.2
    restart: unless-stopped
    ports:
      - "8501:8501"
    volumes:
      - ./data:/data
