name: inspect_frequency
display_name: Inspect Frequency
description: Compute a normalized mean amplitude spectrum from preview traces.
category: inspection
input_type: su
output_type: json
approval_level: auto
parameters:
  max_traces: {type: int, required: false, default: 200, minimum: 1, maximum: 1000}
